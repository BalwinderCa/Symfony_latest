{% extends '@UserBundle/layout.html.twig' %}

{% block fos_user_content %}
    <div class="pad col-md-offset-4 col-md-4">
        {{ form_start(form, {'attr': {'class': 'password_reset'}}) }}
        
        <div>
            {% if app.session.flashbag.has('success') %}
                <p class="alert alert-success">
                    {{ app.session.flashbag.get('success')[0] }}
                </p>
            {% endif %}
            
            <div class="form-group label-floating is-empty">
                {{ form_row(form.password, {'attr': {'class': 'form-control'}}) }}
                
                {% if form.password.vars.errors is not empty %}
                    <div class="error-form">
                        {% for error in form.password.vars.errors %}
                            <p class="alert alert-danger">{{ error.message }}</p>
                        {% endfor %}
                    </div>
                {% endif %}
            </div>

            <div class="form-group label-floating is-empty">
                {{ form_row(form.confirmPassword, {'attr': {'class': 'form-control'}}) }}
                
                {% if form.confirmPassword.vars.errors is not empty %}
                    <div class="error-form">
                        {% for error in form.confirmPassword.vars.errors %}
                            <p class="alert alert-danger">{{ error.message }}</p>
                        {% endfor %}
                    </div>
                {% endif %}
            </div>
        </div>

        <div>
            <input class="btn btn-primary btn-raised col-md-12" type="submit" value="{{ 'Reset Password'|trans }}" />
        </div>

        {{ form_end(form) }}
    </div>
{% endblock %}
